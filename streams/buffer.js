const buf = Buffer.from("hello")

console.log(buf.toJSON())


// Buffer é o meio nativo do node de 'trocar/tratar/armazenar", informações na memoria. Que logo que é finalizado, é eliminado da memoria.

